notification after theme switch
hide laptop stuff like brightness and powerprofiles




-- nixos
make the power button not turn the system off immediatly
readme how to add custom host + flake.nix stuff
add support for hibernation
use cpupower command
custom nix modules
set users in configuration.nix
add a nxre-auto command
dotfiles config through nixos
overhaul nixos config
create custom iso
hashed user password
maybe add disko

-- hyprland
fix permissions for hyprpicker, obs and hyprlock
improve animations
configure & activate hyprlock and hypridle when lua support is added for them
hyprland monitors through nixos
make super+shift+w open alternative web browser
when switched to lua make hyprland check for monitors.lua if the file doesnt exist copy from nixos or something

-- themes
main add the 16 colors for galaxy and terra-castle
waybar modern-v2 check bluetooth colors on the tablet + other bluetooth stuff

-- statusbar
eww hide battery if no battery exists
show vpn, gpu, cpu usage, etc
fix waybar bluetooth icons
gnome like waybar
time and date in one module without clicking on it

-- menu/theme system
gtk theme
auto set waybar theme and layout when setting a theme
add the possiblity to change waybar layout and separate menu for position
new theme idea frameless
fix legacy waybar
btop theme
custom wifi menu
when activating init ask if you want to restore existing dotfiles
add a file that explains how the theme system works
add an option to use a seperate colors for nvim
add more folders to existing-dotfile-folders-fix when ready
make the theme system apply the wallpaper regardless of bg format
fix comments in theme function
if a theme is switching and returns errors make it more clear (popup menu with error)
move delete and reapply to apply/change theme
waybar make steam popup text not have a shadow
what if the files with core, theme or waybar theme names don't exist + if waybar theme missing and the theme support gomplate fix it idk
add a update option to the main menu that updates dotfiles and more from github + backs up the old files
when installing nixos restoring dotfiles through the menu will cause an error because hyprland and etc are no running fix that
play sound when menus apear maybe?
add submenu for waybar stuff e.g. bluetooth, etc
overhaul fastfetch
prevent hyprland from running create custom home folders script every time you restart
menu.sh make func vars local if only used in a function
replace exit ifs with one liners
help popup will eventually disable the rofi theme because the help popup uses #error-message
need to check more edge cases e.g. dotfile folder or active_dotfiles folder not existing, etc
update power menu for x11
add var for some programs so they coulb be easily replaced e.g. wl-copy, etc
update legacy theme because they don't have active_theme dir
rofi make selected text black to match waybar
add a extra confirmation theme that is vertical for rofi
use different fonts
what will happen if there are no nvim or kitty sockets and you run kitty script
check if waybar launches cava after restart
add icons or images to when selecting a theme
add the ability to disable kitty & nvim sockets
put openMenu() arguments into variables to make it easier to understand + make all cars there "${}"
power profiles menu make the current power profiles show above
maybe add a menu for volume
rofi shadow
gomplate add the posiblity to disable shadows for waybar and rofi if rofi has shadows add by then
add the cli-run-safe feature
when pkgs e.g. brightnessctl don't exist hide or mark them with (disabled) on the menu
make hyprland option where you can do bunch of stuff with workspaces e.g. change monitor work space + add numpad theme for rofi

-- terminal
add explanation to sockets in zsh
replace paths with path vars

-- nvim
use terminal colors
make yazi open correct editor
customize colors: barbar, nvim itself, lualine
show errors on lualine
on/off autocomplete
disable autocomplete in .conf
better Lazy.nvim menu (rounded borders)
keybinds to show or hide full file path
change color of auto suggestion from green to something else
add a help menu
reorganize startpage and config.lua
test theme switching
toggle for wrap
don't auto add cpp headers when using autocomplete
custom menu to show all keybinds toggled by a keybind
replace [*] with something else in luabar inactive split window
make inactive #ifdefs easier to read
spell checker
mg979/vim-visual-multi
smoka7/multicursors.nvim
different color brackets {[({{}}{{{}}{{()}}})]}
nvim hover split windows to focus

-- filemanager
yazi color scheme
make alt+h,l switch between tabs + alt+j,k to open and close tabs

-- misc
configure vscode
satty
readme
configure zen
fix installation + improve cheatsheet
configure cava, mangohud, obs-studio, vscode and add them to core
make a text file with all keybindings
add vscode plugins to packages.nix
split nixos cheatsheet into 2 different files + rename it correct
install termusic



waybar sound cava thing idea 
when music is playing: 8x bars ->  ▁▂▃▄▅▆▇█  ▁▂▃▄/35%/▅▆▇█
when no music is playing:  35%, 󰝛 off

    "modules-center": [
        "custom/cava"
    ],

    "custom/cava": {
        "format": "{}",
        "exec": "~/.config/scripts/waybar/cava.sh"
    },

  /* Right modules */
#custom-power-profiles, #bluetooth, #pulseaudio, #network, #custom-cava {
  color: @foreground;
  padding: 0px 9px;
}


